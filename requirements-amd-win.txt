# AMD GPU requirements (ROCm 7.2.1, Windows)
# Uses the universal ROCm package from AMD, which requires Python 3.12.
# Versions are pinned to prevent pip from preferring newer CPU-only wheels from PyPI.

--find-links https://repo.radeon.com/rocm/windows/rocm-rel-7.2.1/

rocm-sdk-core==7.2.1
rocm-sdk-devel==7.2.1
rocm-sdk-libraries-custom==7.2.1
rocm==7.2.1
torch==2.9.1+rocm7.2.1
torchvision==0.24.1+rocm7.2.1
torchaudio==2.9.1+rocm7.2.1
