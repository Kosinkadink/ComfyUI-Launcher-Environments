# AMD GPU requirements (ROCm, RDNA 4 - RX 9000 series)
# Versions are pinned to prevent future pytorch releases from silently changing built environments.

--index-url https://repo.amd.com/rocm/whl/gfx120X-all/
--extra-index-url https://pypi.org/simple

torch==2.10.0+rocm7.12.0
torchvision==0.25.0+rocm7.12.0
torchaudio==2.10.0+rocm7.12.0
